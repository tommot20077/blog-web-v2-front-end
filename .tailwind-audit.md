# Tailwind Utility Class Audit — 全站盤點

**日期**: 2026-07-19
**範圍**: `src/**/*.vue`（排除 `blog-v2-design/`、`blog-v2-1-design/`、`node_modules/`、`dist/`、`e2e/`）
**根因**: `src/index.css` 沒有 `@tailwind base/components/utilities` 指令，只 `@import` 手寫設計系統 CSS
（`src/assets/design/{styles,views,search,settings}.css`）。`tailwind.config.js` / `postcss.config.js`
設定正常，但 Tailwind 的 JIT 從未實際產生任何 utility class 的 CSS 規則。已用 grep 逐一驗證：
`.flex` `.rounded-full` `.animate-pulse` `.absolute` `.fixed` 等在四份設計 CSS 檔中**完全不存在**。

## 總結

- 掃了 **69 個** `.vue` 檔（`find src -name "*.vue" -type f | wc -l` = 69）
- 其中 **22 個**受影響（存在未被任何自訂 CSS 覆蓋的 Tailwind class）
- 47 個乾淨（只用專案自訂語意 class，或該檔 `<style scoped>` / 設計系統 CSS 已覆蓋同名 class）

### 重要修正因子：Live vs Dead code

用 `Grep` 追蹤每個受影響元件是否真的被任何 View / `App.vue` import 並渲染（而非只被自己的
`*.test.ts` 引用），22 個受影響檔案中：

- **8 個是「活的」**（實際會被使用者看到，優先修）
- **14 個是「孤兒元件」**（目前沒有任何 view/component import，只有自己的單元測試在用 —
  推測是重構/換版遺留或尚未接線的 WIP 元件，使用者現在看不到，但未來一旦被接上就會立刻壞掉）

| 分類 | 檔案 |
|---|---|
| **LIVE**（App.vue 全域 or 已被 view import） | `ToastContainer.vue`, `ToastItem.vue`, `MobileBottomNav.vue`, `ThemeSwitcher.vue`（被 MobileBottomNav 引用）, `EditorMetaSidebar.vue`, `EditorToolbar.vue`（皆被 `EditorView.vue` 引用）, `PasswordRulesChecklist.vue`, `PasswordStrengthMeter.vue`（被 `RegisterView.vue` / `SettingsView.vue` 引用） |
| **DEAD / orphaned**（僅 `*.test.ts` 引用，src 內無任何 import） | `ArticleCard.vue`, `ArticleCardSkeleton.vue`, `FilterBar.vue`, `StickyNote.vue`, `StickyNoteBoard.vue`, `WeatherWidget.vue`, `EditorPane.vue`, `EditorPreview.vue`, `components/Navbar.vue`（注意：與 `components/layout/NavigationBar.vue` 不同檔，後者是目前實際使用中的主導覽列，本身乾淨）, `PillButton.vue`, `SkeletonBlock.vue`, `SkeletonCircle.vue`, `SkeletonLine.vue`, `SkeletonPulse.vue` |

驗證方式：`Grep "ComponentName" src --glob "*.vue"`，只有自己的 `.test.ts` 命中 = dead。
（例：`EditorView.vue` 的 CodeMirror 掛載與 preview 渲染是直接呼叫 composable 內聯完成，
並未使用 `EditorPane.vue` / `EditorPreview.vue` 這兩個元件。）

---

## 特別關注：Toast 元件（使用者回報案例，已證實）

`src/components/ui/ToastContainer.vue:9`
```html
<div class="fixed top-24 right-6 z-[100] flex flex-col gap-3 max-md:top-4 max-md:right-4 max-md:left-4">
```
無 `<style>` 區塊、無任何自訂 CSS 覆蓋。`fixed`/`top-24`/`right-6`/`z-[100]`/`flex`/`flex-col`/`gap-3`
全部失效 → toast 容器沒有 `position:fixed`，直接落在文件流中（通常等同畫面左上角），
無 z-index、無 flex 排列。**與使用者回報症狀完全吻合。**

`src/components/ui/ToastItem.vue:22`
```html
<div class="flex items-stretch gap-3 rounded-2xl border px-4 py-3 shadow-lg min-w-[280px] max-w-[400px] transition-all duration-300" ...>
```
`:28` `w-1 rounded-full shrink-0`（左側色條，含 `bg-green-500`/`bg-red-500`/`bg-yellow-500`/`bg-blue-500` 動態 class）
`:33` `flex-1 text-sm font-medium py-0.5`
`:40` `shrink-0 opacity-40 hover:opacity-100 transition-opacity text-sm leading-none self-start pt-0.5`

全部是未編譯的 Tailwind class：卡片沒有 `flex`、沒有圓角、沒有陰影、沒有 padding、沒有
min/max-width 限制，左側色條指示器沒有顏色沒有形狀。這就是「toast 卡片出現在左上角且無卡片樣式」的根因。

---

## 完整清單（依嚴重程度排序，LIVE 優先）

### 嚴重（SEVERE）— LIVE 元件

| file:line | 壞掉的 class 片段 | 症狀 |
|---|---|---|
| `src/components/ui/ToastContainer.vue:9` | `fixed top-24 right-6 z-[100] flex flex-col gap-3` | 容器無定位/無 z-index/無排列，飄到左上角 |
| `src/components/ui/ToastItem.vue:22` | `flex items-stretch gap-3 rounded-2xl border px-4 py-3 shadow-lg min-w-[280px] max-w-[400px]` | 卡片無圓角/陰影/邊框/寬度限制/排列 |
| `src/components/ui/ToastItem.vue:28` | `w-1 rounded-full shrink-0` + `bg-{color}-500`（動態） | 左側色條指示器無形狀無顏色 |
| `src/components/ui/ToastItem.vue:33,40` | `flex-1 text-sm ...` / `shrink-0 opacity-40 hover:opacity-100 ...` | 訊息排版、關閉鈕透明度回饋失效 |
| `src/components/layout/MobileBottomNav.vue:46` | `fixed bottom-0 left-0 right-0 z-50 border-t` | 手機底部導覽列不再固定，直接流入內文 |
| `src/components/layout/MobileBottomNav.vue:49` | `absolute top-1 right-2` | 內嵌的 ThemeSwitcher 定位失效 |
| `src/components/layout/MobileBottomNav.vue:52,57` | `flex items-center justify-around py-2` / `flex flex-col items-center gap-0.5 px-4 py-1` | 四個分頁 tab 排列全毀（垂直堆疊而非橫排） |
| `src/components/layout/MobileBottomNav.vue:58` | `:class` 三元 `text-[var(--accent)]` / `opacity-50` | 目前分頁的 active 顏色標示消失 |
| `src/components/layout/MobileBottomNav.vue:62,66,70,74` | `w-5 h-5`（4 個 svg icon） | 導覽圖示無尺寸（可能巨大或跑版） |
| `src/components/editor/EditorMetaSidebar.vue:106` | `w-72 shrink-0 flex flex-col p-4 bg-white/60 backdrop-blur-md border-l border-white/80 dark:bg-white/8 ...` | 編輯器右側欄無寬度/無背景/無邊框/無排列 |
| `src/components/editor/EditorMetaSidebar.vue:124,140,162,166,185` | 多處 `flex flex-col/wrap gap-*` | 封面/分類/標籤各區塊排列全毀 |
| `src/components/editor/EditorMetaSidebar.vue:131,133,136` | `relative rounded-xl overflow-hidden` / `w-full h-32 object-cover` / `absolute top-1 right-1 bg-black/50 ...` | 封面圖預覽無裁切/無圓角，移除鈕無定位無背景 |
| `src/components/editor/EditorMetaSidebar.vue:142,189,206,235` | `rounded-full bg-[var(--accent)] ...` / `rounded-full bg-white/60 ...` / `w-full ... rounded-xl bg-white/60 border ...`（輸入框、標籤 pill、上傳鈕） | 表單欄位、標籤 pill 全部無樣式（原生瀏覽器外觀） |
| `src/components/editor/EditorMetaSidebar.vue:151` | `hidden`（file input） | 原生檔案選擇器直接顯示，UI 跑版 |
| `src/components/editor/EditorMetaSidebar.vue:201,213` | `relative` / `absolute z-10 w-full mt-1 bg-white ... rounded-xl shadow-lg` | 標籤建議下拉選單失去浮動定位，直接撐開版面而非覆蓋層 |
| `src/components/editor/EditorToolbar.vue:48` | `flex flex-wrap items-center gap-1 px-2 py-1.5 bg-white/60 backdrop-blur-md border-b ...` | 工具列按鈕全部無排列、無背景、無底線 |
| `src/components/editor/EditorToolbar.vue:55` | `px-2 py-1 rounded-lg hover:bg-white/40 min-w-[2rem] text-center` | 工具列按鈕無圓角、無最小寬度、hover 無回饋 |
| `src/components/editor/EditorToolbar.vue:62` | `w-px h-5 bg-gray-300 dark:bg-gray-600 mx-0.5` | 群組分隔線無寬高無顏色（等於消失） |
| `src/components/layout/ThemeSwitcher.vue:16` | transition class `rotate-[-90deg] scale-50 / rotate-0 scale-100 / opacity-*`（含拼字錯誤的 `position-absolute`，本來就不是合法 Tailwind class） | 深/淺色圖示切換動畫（旋轉+縮放+淡入）完全沒有效果 |
| `src/components/layout/ThemeSwitcher.vue:18` | `text-[#FF8D28]`（IconSun 顏色） | 太陽圖示遺失橘色，改用父層預設文字色 |
| `src/components/auth/PasswordRulesChecklist.vue:15,21,30,39,48,57` | `flex flex-col gap-1 mt-2 text-xs` / 5× `flex items-center gap-2` | 規則清單無排列，仍有預設 `<ul>` 項目符號 |
| `src/components/auth/PasswordRulesChecklist.vue:22,31,40,49,58` | `:class` 三元 `text-green-500` / `text-gray-400` | 達成/未達成規則完全同色，無法辨識 |
| `src/components/auth/PasswordStrengthMeter.vue:43,46` | `h-1.5 w-full rounded-full bg-gray-200/30` / `h-full rounded-full ...` + `:class` `bg-{red/orange/green}` | 強度條高度為 0、背景透明、顏色失效（等於整條看不到） |

### 嚴重（SEVERE）— DEAD/孤兒元件（目前無人看得到，但落地就壞）

| file:line | 壞掉的 class 片段 | 症狀 |
|---|---|---|
| `src/components/article/ArticleCard.vue:18` | `flex flex-col rounded-[24px] border cursor-pointer group hover:scale-[1.02] ...` | 文章卡片無排列/無圓角/無邊框/無 hover 效果 |
| `src/components/article/ArticleCard.vue:27,30,32,37,46,51-70` | 多處 `flex`/`text-xl font-bold`/`text-xs opacity-50`/`rounded-full` 等 | 圖片、標題、meta、標籤、統計列全數跑版 |
| `src/components/article/ArticleCardSkeleton.vue:3,6,10,23,56,59,64,69` | `flex flex-col rounded-[24px]` / `animate-pulse` / `flex items-center gap-3` / `rounded-full`（tag 骨架） | 骨架卡片無圓角、無 shimmer 動畫、author/tag 骨架直排而非橫排 |
| `src/components/article/FilterBar.vue:34,38,45-46,53,56,59-71,83,94` | `flex justify-between ...` / `rounded-full border` / active 三元 `bg-[var(--ink)] text-[var(--bg)]` 等 | 篩選列排列、分類 pill 形狀、grid/list 切換 active 色全毀 |
| `src/components/common/StickyNote.vue:37,42,45,50,52` | `relative w-48 h-48 ... rounded-xl shadow-sm hover:...` / `absolute -top-3 ...` / `text-[10px]` | 便利貼卡片無尺寸無形狀無 hover，貼紙裝飾無定位 |
| `src/components/common/StickyNoteBoard.vue:34,35,37,43` | `flex flex-col items-center` / `flex justify-between items-end border-b` / `rounded-full border border-current` / `flex flex-wrap justify-center gap-6` | 整個留言板版面、標題列、按鈕、卡片排列全毀 |
| `src/components/common/WeatherWidget.vue:9,14,17,22,25` | `flex items-center gap-3 ... rounded-full border` / `animate-pulse w-5 h-5 bg-gray-400/50 rounded-full` / `w-5 h-5 text-accent` / `flex flex-col` / `flex items-baseline gap-1` | 天氣小工具無膠囊形狀、loading 骨架無形狀、圖示無尺寸無色 |
| `src/components/editor/EditorPane.vue:35` | `h-full overflow-auto font-mono text-sm` | CodeMirror 容器無高度，可能整個塌陷為 0px |
| `src/components/editor/EditorPreview.vue:14` | `h-full overflow-auto px-6 py-4 bg-white/40 dark:bg-white/5` | 預覽區無高度無背景（`prose` 本身有效，見下方註記） |
| `src/components/Navbar.vue:38` | `flex items-center gap-4 px-6 py-3 border-b ... bg-white/60 backdrop-blur-md` | 導覽列無排列無背景無底線 |
| `src/components/Navbar.vue:65` | `ml-1 inline-flex items-center justify-center px-1.5 py-0.5 rounded-full bg-red-500 text-white` | 待審核數量徽章無膠囊形狀無紅色背景 |
| `src/components/ui/PillButton.vue:30,31,35-36,38` | `w-8 h-8 rounded-full flex items-center justify-center` / `px-5 py-2 rounded-full` / active `bg-[var(--ink)] text-[var(--bg)] shadow-md` | 按鈕/分頁 tab 無形狀無 active 狀態顏色 |
| `src/components/ui/SkeletonBlock.vue:10` | `rounded-xl` | 骨架方塊直角，非圓角卡片 |
| `src/components/ui/SkeletonCircle.vue:9` | `rounded-full` | 頭像骨架是方形，不是圓形 |
| `src/components/ui/SkeletonLine.vue:10` | `rounded-md` | 骨架線條直角 |
| `src/components/ui/SkeletonPulse.vue:2` | `animate-pulse` | loading 動畫完全靜止，無 shimmer |

### 輕微（MINOR）— 僅間距/字重微調，影響輕微

| file:line | 壞掉的 class 片段 | 症狀 |
|---|---|---|
| `src/components/common/WeatherWidget.vue:23,26,27` | `text-xs/text-sm/text-[10px] font-medium/bold tracking-*` | 文字尺寸/字重/字距微調遺失 |
| `src/components/editor/EditorMetaSidebar.vue:143,173,218,222` | `opacity-60` / `rounded`（checkbox） / `hover:bg-gray-100` / `text-gray-400 ml-1` | 上傳中透明度、checkbox 圓角、hover 高亮、小標籤色差 |
| `src/components/editor/EditorPreview.vue:17` | `prose-sm max-w-none dark:prose-invert` | `prose` 本身在 `views.css` 有定義有效，只有這幾個修飾字失效，影響低 |
| `src/components/Navbar.vue:40,43,49,52,60,73,81` | `font-medium hover:text-[var(--accent)] transition-colors`（8 處導覽連結） | 只失去 hover 顏色回饋，預設文字仍可見 |
| `src/components/ui/PillButton.vue:26,39,41` | `transition-all duration-300 font-medium` / `hover:bg-black/5 dark:hover:bg-white/10` / `cursor-not-allowed opacity-30` | 過場動畫、hover 背景、disabled 視覺提示遺失 |
| `src/components/layout/ThemeSwitcher.vue:12` | `relative p-2 rounded-full hover:bg-black/5 dark:hover:bg-white/10 cursor-pointer` | hover 時無圓形背景（預設無 hover 時外觀正常，因 `.theme-switcher` 自訂 CSS 已固定 40×40 + flex 置中） |

---

## 乾淨（未受影響）檔案清單（47 個，摘要）

App.vue、ActionBar.vue、ArticleHighlightPanel.vue、ArticleTextSelectionToolbar.vue、
CommentSection.vue、ReactionFooter.vue、RelatedArticlesSection.vue、AuthFormLayout.vue、
CommentForm.vue、CommentItem.vue、IconMoon.vue、IconSun.vue、HeroMarquee.vue、
HeroSection.vue、HotTagsSection.vue、LatestArticlesSection.vue、TrendingSection.vue、
AppFooter.vue、Footer.vue（僅佔位文字）、NavigationBar.vue（目前實際使用中的主導覽列）、
SettingFieldGroup.vue、SettingSaveToast.vue、SettingToggle.vue、FollowButton.vue、
FormField.vue、AdminReviewView.vue、ArchiveView.vue、ArticleDetail.vue、ArticleList.vue、
AuthorView.vue、BookmarksView.vue、EditorView.vue、ForgotPasswordView.vue、Home.vue、
HomeView.vue、LoginView.vue、MyArticlesView.vue、NotFoundView.vue、RegisterView.vue、
ResetPasswordView.vue、SearchView.vue、ServerErrorView.vue、SettingsView.vue、
StatsView.vue、TagsIndexView.vue、TagView.vue、VerifyEmailView.vue

這些檔案只用專案自訂 kebab-case 語意 class（如 `auth-form`、`nav-*`、`sk-pulse`、`prose` 等），
且都已在對應 `<style scoped>` 或 `src/assets/design/*.css` / `src/index.css` 中有定義
（例：`.sk-pulse`/`.sk-card`/`.sk-thumb`/`.sk-body`/`.sr-only` 定義於 `src/index.css:17-30,73`；
`.prose` 系列定義於 `src/assets/design/views.css:157-168`）。

---

## 方法論

四個 general-purpose subagent 並行讀取全部 65 個檔案（分 4 批，每批 16-17 檔），對每個
`class="..."` 與 `:class="..."` binding 逐一分類為「Tailwind utility」或「專案自訂」，
再對每個 Tailwind token 用 grep 檢查是否被該檔自己的 `<style scoped>` 或四份設計 CSS 檔
以字面 selector 定義過。剩餘 4 個檔案（`ToastContainer.vue`、`ToastItem.vue`、
`PasswordRulesChecklist.vue`、`PasswordStrengthMeter.vue`）由我本人直接讀取驗證
（即使用者原始回報案例）。事後我用獨立 grep 抽查了 `.flex`/`.rounded-full`/`.animate-pulse`/
`.absolute`/`.fixed`/`.sk-pulse`/`.sr-only` 等關鍵字在四份設計 CSS + `index.css` 中的定義情況，
與 subagent 結論一致。另外針對 22 個受影響檔案逐一用 `Grep` 確認是否被任何 view/component
實際 import，區分 live/dead，以利修法優先序判斷。
